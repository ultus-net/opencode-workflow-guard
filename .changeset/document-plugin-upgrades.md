---
"opencode-workflow-guard": patch
---

Document OpenCode's supported plugin install and upgrade command with explicit package versions so upgrades do not remain on a previously cached `@latest` release.
