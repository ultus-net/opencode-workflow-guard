---
"opencode-workflow-guard": patch
---

Clarify Workflow Guard's role as a deterministic policy and enforcement layer rather than an agent harness, distinguish optional context features from core guardrails, and fix the TUI project-options menu so selecting an option persists the toggle.
