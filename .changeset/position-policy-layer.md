---
"opencode-workflow-guard": patch
---

Clarify Workflow Guard's role as a deterministic policy and enforcement layer rather than an agent harness, and distinguish optional memory and learning context features from core guardrails.
