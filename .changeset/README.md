# Changesets

Hello and welcome! This folder contains Changesets files that describe the changes in this repo.

You can create a new changeset with:

```bash
npx changeset
```

For more info, see the [Changesets repo](https://github.com/changesets/changesets).
