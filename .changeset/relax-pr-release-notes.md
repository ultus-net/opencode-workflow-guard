---
"opencode-workflow-guard": patch
---

Raise the default subagent mutation budget to 100 and accept conventional release-information headings in PR descriptions without requiring the literal word `Changelog`.
