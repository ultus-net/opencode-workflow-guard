---
"opencode-workflow-guard": patch
---

Fix the TUI project-options menu so selecting an option actually toggles and persists the setting, and enable the title-settle workaround by default while preserving `titleSettleWorkaround: false` as an explicit opt-out.
